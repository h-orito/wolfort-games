query CharachipDetail($id: ID!) {
  charachip(id: $id) {
    id
    name
    descriptionUrl
    canChangeName
    designer {
      name
    }
    charas {
      id
      name
      size {
        width
        height
      }
      images {
        type
        url
      }
    }
  }
}
