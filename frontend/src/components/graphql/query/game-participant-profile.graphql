query GameParticipantProfile($participantId: ID!) {
  gameParticipantProfile(participantId: $participantId) {
    participantId
    name
    entryNumber
    isGone
    profileImageUrl
    introduction
    followsCount
    followersCount
    isPlayerOpen
    playerName
  }
}
